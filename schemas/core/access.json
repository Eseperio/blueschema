{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Access Rule Schema",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "description": "Role required to execute the action. Should reference a role defined in accessControl.roles"
    },
    "permission": {
      "type": "string",
      "description": "Permission required to execute the action. Should reference a permission defined in accessControl.permissions or accessControl.roles[].permissions"
    },
    "prompt": {
      "type": "string",
      "description": "Prompt that must be satisfied"
    }
  },
  "anyOf": [
    { "required": ["role"] },
    { "required": ["permission"] },
    { "required": ["prompt"] }
  ],
  "additionalProperties": false
}
